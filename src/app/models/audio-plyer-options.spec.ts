import { AudioPlyerOptions } from './audio-plyer-options';

describe('AudioPlyerOptions', () => {
  it('should create an instance', () => {
    expect(new AudioPlyerOptions()).toBeTruthy();
  });
});
